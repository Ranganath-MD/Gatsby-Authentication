import Typography from "typography";

// Adding the typography to the app
import funstonTheme from 'typography-theme-funston'
const typography = new Typography(funstonTheme);
export default typography;